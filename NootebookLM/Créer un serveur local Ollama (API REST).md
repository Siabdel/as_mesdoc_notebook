



# Créer un serveur local Ollama (API REST)

> Created le 8 Janvier 2026
>
> Author : SA





* créer un **serveur local Ollama (API REST)**
* choisir **les meilleures quantizations pour RTX 3060**
* créer un **serveur local Ollama (API REST)**
* comparer **mistral vs deepseek vs qwen** pour usage **strictement métier**
* brancher Ollama sur **PDF (factures, budgets) avec RAG**
*  **Analyser des PDF (factures/budgets) avec Ollama**
* 🧠 **RAG local (FAISS / Chroma)**
* ⚙️ **Prompt métier ultra-strict (règles business)**
* 🚀 **Benchmark RTX 3060 (VRAM / perf)**
*  **Docker-compose complet** (Django + Postgres + Redis + Qdrant + MinIO + n8n + LLM + Embeddings + OCR)
* 🔹 **Un frontend Vue.js chat RAG** fonctionnel
*  🔹 **Le script Python `save_to_minio()`**
*  🔹 **Le service embeddings complet (FastAPI)**
*  🔹 **Le service OCR PyMuPDF complet**
*  🔹 **Le service Whisper complet (FastAPI)**



---

* **Docker-compose complet** (Django + Postgres + Redis + Qdrant + MinIO + n8n + LLM + Embeddings + OCR)
*  🔹 **Un frontend Vue.js chat RAG** fonctionnel
*  🔹 **Le script Python `save_to_minio()`**
*  🔹 **Le service embeddings complet (FastAPI)**
*  🔹 **Le service OCR PyMuPDF complet**
*  🔹 **Le service Whisper complet (FastAPI)**